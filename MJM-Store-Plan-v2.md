## نظرة عامة على المشروع (Executive Summary)

متجر **MJM** هو منصة تجارة إلكترونية متكاملة تستهدف شريحتين رئيسيتين:

- **قطاع الأفراد (B2C)**: لبيع المنتجات الاستهلاكية مثل الأكياس، المياه، الكراتين، وأدوات الضيافة، مع تركيز خاص على البكجات والعروض.
- **قطاع الأعمال (B2B)**: لتقديم حلول تغليف وطباعة خاصة للعلامات التجارية (مطاعم، كافيهات، شركات، مصانع…).

يهدف المشروع إلى:

- **نمو المبيعات الاستهلاكية** مع إبراز المنتجات الأعلى طلباً (خاصة أكياس النفايات والبكجات).
- **اكتساب عملاء شركات** عبر رحلة واضحة وسلسة لطلب عروض الأسعار للطباعة الخاصة.
- **ترسيخ العلامة التجارية MJM** كمرجع موثوق في قطاع المستلزمات والتغليف داخل المملكة.

هذه الوثيقة تمثل **المخطط الفني الشامل (Full-Stack Blueprint)** وتشمل:

- التكديس التقني والمعماري (Tech Stack & Architecture).
- هيكلية المعلومات وتجربة المستخدم (UX & IA).
- نموذج البيانات وقواعد العمل (Data Model & Business Logic).
- توصيف الصفحات بالتفصيل (Page-Level UI/UX Specs).
- لوحة التحكم، التكاملات مع الأنظمة الخارجية، الأداء، الأمان، وخطة التنفيذ الزمنية.

---

## التكديس التقني (Tech Stack & Architecture)

- **الفرونت إند (Frontend)**:
  - **Next.js** (موصى به) أو React لـ:
    - تحسين الـ SEO.
    - دعم SSR / SSG.
  - **Tailwind CSS** لتصميم واجهة متجاوبة (Mobile-First) مع نظام Design System واضح.
  - **React Query** لإدارة البيانات القادمة من الـ API (Caching, Refetching, Status Handling).
  - **Context API أو Zustand** لإدارة حالة السلة وحالة المستخدم (Authentication, Cart State).

- **الباك إند (Backend)**:
  - **NestJS (Node.js)** لبنية منظمة (Modular, DI, Controllers/Services).
  - **PostgreSQL** كقاعدة بيانات علائقية قوية.
  - **Prisma أو TypeORM** كـ ORM لإدارة المخطط والاستعلامات.
  - توثيق الـ API باستخدام **Swagger**.

- **البنية التحتية (Infrastructure)**:
  - استضافة الفرونت: Vercel أو ما يعادله (أو ضمن نفس السيرفر إن تطلب).
  - استضافة الباك إند + قاعدة البيانات: DigitalOcean / AWS / Render.
  - **CDN** للصور والملفات الثابتة (Cloudflare أو بديل) مع نقاط تواجد قريبة من المملكة.

- **خدمات خارجية (3rd Party Integrations)**:
  - دفع: **Moyasar أو Tap** + **Tamara**.
  - شحن: **أرامكس أو ساعي** + **شركة الصندوق للنقل الثقيل** للطلبات الكبيرة.
  - تواصل: **WhatsApp API** للإشعارات ورسائل خدمة العملاء.

---

## هيكلية المعلومات وتجربة المستخدم (UX & IA)

### الأقسام الرئيسية للموقع

- **الرئيسية** (Landing Page مطوّلة).
- **الكتالوج / الأقسام**:
  - أكياس نفايات.
  - كراتين شحن.
  - مياه MJM.
  - أدوات ضيافة.
  - البكجات (Bundles).
- **خدمة الطباعة الخاصة (B2B Custom Printing)**.
- **حسابي**:
  - ملفي الشخصي.
  - عناويني.
  - طلباتي.
  - طلبات الطباعة للشركات.
- **السلة** و **إتمام الطلب (Checkout)**.
- **صفحات معلوماتية**:
  - من نحن.
  - سياسة الشحن والإرجاع.
  - سياسة الخصوصية.
  - الشروط والأحكام.
  - تواصل معنا.

### تجربة الجوال (Mobile-First)

- **Bottom Navigation** ثابت في أسفل الشاشة:
  - الرئيسية.
  - الأقسام.
  - السلة (مع عدّاد).
  - حسابي.
- أزرار CTA بحجم مناسب للمس (Touch Target ≥ 44px).
- تبسيط المحتوى في الجوال واستخدام "عرض المزيد" للنصوص الطويلة.

---

## تفاصيل الصفحات (UI/UX – Page-Level Specifications)

### 1. الصفحة الرئيسية – `Home / Landing`

- **أهداف الصفحة**:
  - تقديم MJM كعلامة تجارية **راقية وموثوقة** منذ اللحظة الأولى.
  - توجيه المستخدم بسرعة نحو أهم المسارات: (تسوق المنتجات، اكتشاف البكجات، طلب طباعة خاصة).
  - إبراز العروض والمزايا التنافسية وعناصر الثقة في إطار بصري أنيق ومتماسك.

- **الهوية البصرية للصفحة**:
  - خلفية نظيفة (فاتحة أو داكنة بسيطة) مع استخدام لون الهوية الأساسي للأزرار والعناوين، ولون ثانوي لـ Badges والعروض.
  - Grid منظم مع مساحات بيضاء واسعة لإحساس بالفخامة والراحة البصرية.
  - حركات ظهور ناعمة للأقسام (Fade-in / Slide-in) أثناء التمرير.

- **Hero Section**:
  - شريط علوي رفيع (Announcement Bar) لرسالة مثل: "شحن مجاني للطلبات فوق 299 ريال".
  - سلايدر بعرض كبير يحتوي على:
    - صورة 3D بخلفية ناعمة غير مزدحمة.
    - عنوان رئيسي (H1) يوضح القيمة (مثال: "مستلزمات تغليف وضيافة بجودة فاخرة وأسعار جملة").
    - نص ثانوي قصير (سطرين) يشرح ما يميز MJM.
    - صف أزرار CTA:
      - زر أساسي ممتلئ: "تسوق المنتجات".
      - زر ثانوي بإطار فقط: "خدمة الطباعة للشركات".
    - شارة صغيرة تحت الأزرار توضح عدد الطلبات/العملاء كعنصر ثقة.

- **قسم الفئات (Interactive Category Grid)**:
  - خلفية أفتح قليلاً من الخلفية العامة لتمييز القسم.
  - شبكة من 4–6 بطاقات؛ كل بطاقة:
    - أيقونة 3D في المنتصف.
    - اسم القسم بخط واضح.
    - وصف قصير جداً (سطر واحد).
  - عند الـ Hover:
    - تكبير بسيط للأيقونة + ظل ناعم + رفع خفيف للبطاقة.
  - في الموبايل: تأثير ضغط (Ripple) خفيف عند اللمس.

- **قسم البكجات (Bundles & Offers)**:
  - في الديسكتوب: شبكة 3 أعمدة؛ في الموبايل: كاروسيل أفقي يمكن سحبه.
  - بطاقة البكج:
    - هيدر ملون باسم البكج وشارة مثل "أفضل توفير".
    - قسم بصري يعرض مكونات البكج (أيقونات/صور صغيرة مع ×3، ×2…).
    - سطر أسعار:
      - السعر قبل الخصم (مخطط بخط رفيع).
      - السعر بعد الخصم بارز.
      - نص صغير "توفر X ريال (Y%)".
    - زر "إضافة البكج للسلة" ممتد تقريباً بعرض البطاقة مع أيقونة سلة.

- **قسم خدمة الطباعة الخاصة (B2B Widget)**:
  - خلفية مختلفة (لون ثانوي فاتح) مع شكل موجي في الأعلى.
  - عرض خطوات 1–2–3 بصرياً:
    - 1) اختر المنتج.
    - 2) ارفع الشعار.
    - 3) استلم عرض السعر.
  - نموذج طلب مبسّط:
    - حقول منظمة في مجموعات (بيانات النشاط / تفاصيل الطلب / رفع الشعار).
    - رفع الشعار عبر Drag & Drop في الديسكتوب، وزر رفع بسيط في الموبايل.
    - زر إرسال عريض بلون الهوية مع نص ثانوي: "نعود إليك خلال 24–48 ساعة".

- **قسم المزايا (Why MJM)**:
  - 3–4 بطاقات موحّدة الشكل:
    - أيقونة Line أو 3D صغيرة.
    - عنوان قصير (كلمتين–ثلاث).
    - سطر شرح بسيط.

- **قسم التقييمات (Testimonials)**:
  - خلفية خفيفة مختلفة عن باقي الأقسام.
  - كاروسيل يعرض:
    - اقتباس قصير.
    - اسم العميل/النشاط + المدينة.
    - تقييم نجوم إن توفر.

- **قسم الشركاء (Partners Logos)**:
  - شريط من شعارات العملاء/الشركاء بشكل Monochrome (رمادي) حتى لا يكسر الهوية اللونية.

- **Footer**:
  - خلفية داكنة ونص فاتح.
  - 3–4 أعمدة:
    - روابط أساسية (من نحن، السياسات…).
    - معلومات التواصل (هاتف، واتساب، إيميل، موقع).
    - عناصر الثقة (معروف، منصة الأعمال، وسائل الدفع).
  - أيقونة واتساب 3D عائمة أعلى الفوتر للاتصال السريع.

---

### 2. صفحة الكتالوج / الأقسام – `Categories / Catalog`

- **أهداف الصفحة**:
  - تمكين المستخدم من استكشاف المنتجات بسهولة حسب الفئة مع تجربة تصفح مريحة وجميلة.
  - إبراز المنتجات بوضوح مع فلاتر أنيقة وبسيطة.

- **الفلاتر (Filters)**:
  - في الديسكتوب: عمود جانبي ببطاقات لكل مجموعة فلاتر (فئة فرعية، سعر، نوع الاستخدام، عروض).
  - في الموبايل: زر "تصفية" يفتح Drawer جانبي أو من أسفل.
  - استخدام Chips ملوّنة خفيفة للفلاتر المفعّلة مع إمكانية إزالتها بنقرة.

- **شبكة المنتجات (Products Grid)**:
  - بطاقات بظل خفيف وحواف ناعمة.
  - كل بطاقة:
    - صورة بنسبة 4:3 ثابتة مع خلفية نظيفة.
    - اسم المنتج بخط متوسط لا يزيد عن سطرين.
    - سطر أسعار:
      - السعر الحالي بلون أساسي.
      - السعر السابق مشطوب بلون هادئ.
      - Badge صغيرة لعروض مثل "ثاني قطعة بريال".
    - زر "إضافة للسلة" أسفل البطاقة، بلون ثانوي؛ عند الـ Hover يتحول للون الأساسي.
    - الضغط على الصورة أو الاسم يفتح صفحة تفاصيل المنتج.

- **الفرز (Sorting)**:
  - Dropdown أو Tabs بخلفية خفيفة:
    - "الأحدث".
    - "السعر من الأقل للأعلى".
    - "السعر من الأعلى للأقل".
    - "الأكثر مبيعاً".

- **حالة لا توجد نتائج**:
  - أيقونة توضيحية (صندوق فارغ).
  - نص: "لا توجد منتجات مطابقة لخياراتك".
  - زر "مسح الفلاتر" أو "عرض كل المنتجات".

---

### 3. صفحة تفاصيل المنتج – `Product Details`

- **أهداف الصفحة**:
  - تقديم كل المعلومات التي يحتاجها العميل لاتخاذ قرار الشراء بثقة.
  - جعل تجربة استعراض المنتج قريبة من متاجر العلامات العالمية.

- **التخطيط العام**:
  - في الديسكتوب: عمود للصور، عمود للتفاصيل.
  - في الموبايل: الصور بالأعلى ثم التفاصيل، مع تباعد مريح.

- **معرض الصور**:
  - صورة رئيسية كبيرة بحواف ناعمة وخلفية فاتحة.
  - صور مصغرة أسفلها/جانبها مع Border للعنصر النشط.
  - دعم Zoom والتمرير باللمس (Swipe) في الموبايل.

- **منطقة التفاصيل**:
  - اسم المنتج (H1/H2) واضح وكبير.
  - وصف تسويقي قصير يركّز على الفائدة.
  - أسعار:
    - السعر الحالي بارز بلون الهوية.
    - السعر القديم مشطوب بلون أهدأ.
    - Badge لنسبة الخصم بجوار السعر.
  - شارة للعرض الخاص (مثل "القطعة الثانية بريال") فوق أو بجوار العنوان.

- **تفاصيل المواصفات**:
  - قائمة نقطية أو جدول:
    - الوزن/السعة.
    - الأبعاد.
    - المادة.
    - عدد القطع لكل كرتون (إن كان بيع جملة).

- **منطقة الشراء**:
  - مكوّن كمية (+ / - / رقم) داخل حاوية واحدة أنيقة.
  - زر "إضافة للسلة" كبير وممتد تقريباً بعرض العمود.
  - تحت الزر سطر صغير يشرح العرض (مثلاً: "عند شراء قطعتين، تحصل على الثانية بـ 1 ريال").

- **معلومات الشحن والتوفر**:
  - نص يوضح حالة المخزون بألوان مختلفة حسب الحالة.
  - تقدير وقت التوصيل + إشارة "شحن مجاني فوق 299 ريال" عند تحقق الشرط.

- **تبويبات المحتوى**:
  - Tabs: "الوصف التفصيلي"، "المواصفات الفنية"، "التقييمات".
  - انتقال ناعم بين التبويبات بحركات خفيفة.

- **منتجات ذات صلة**:
  - شريط بعنوان "منتجات مشابهة" أو "قد تحتاج أيضاً" بأسفل الصفحة.

---

### 4. صفحة البكجات – `Bundles`

- **الهدف الجمالي**:
  - إبراز البكجات كبطل أساسي في استراتيجية التسعير والتوفير.
  - جعل واجهة البكجات مختلفة بصرياً عن المنتجات الفردية.

- **بطاقة البكج**:
  - هيدر ملون باسم البكج وشارة "وفر X%".
  - قسم بصري يعرض أيقونات/صور المنتجات المكوِّنة للبكج مع الكمية (×3، ×2…).
  - قسم التوفيرات:
    - السعر الإجمالي لو تم الشراء منفصلاً.
    - السعر كبكج.
    - نص "وفرت X ريال".
  - تذييل البطاقة:
    - زر "تفاصيل البكج".
    - زر "إضافة البكج للسلة".

- **صفحة تفاصيل البكج (اختيارية)**:
  - جدول فيه:
    - اسم المنتج.
    - الكمية في البكج.
    - سعره منفرداً.
    - مساهمته في سعر البكج.
  - ملخص بصري (Bar/Progress) يوضح قيمة التوفير كنسبة ومبلغ.

---

### 5. صفحة خدمة الطباعة الخاصة – `B2B Custom Printing`

- **الانطباع العام**:
  - صفحة تشبه "حلول أعمال" (Business Solutions):
    - مساحات واسعة.
    - نصوص مختصرة وواضحة.
    - صور موك أب احترافية.

- **أقسام رئيسية**:
  - **Hero صغير**:
    - عنوان مثل: "حلول تغليف وطباعة مخصصة لعلامتك التجارية".
    - نص يوضح أن الخدمة موجهة للشركات والمشاريع.
  - **أنواع المنتجات**:
    - بطاقات كبيرة لكل نوع (أكياس، كراتين، مياه…) مع صورة 3D واقعية ووصف قصير.
  - **معرض أعمال (Portfolio)**:
    - شبكة/كاروسيل لصور منتجات مطبوعة مع Overlay خفيف باسم العميل (إن أمكن).

- **نموذج طلب الطباعة**:
  - تقسيم الحقول داخلياً إلى:
    - بيانات النشاط (اسم، نوع النشاط، سجل تجاري اختياري).
    - تفاصيل الطلب (نوع المنتج، الكمية المتوقعة، ملاحظات).
    - رفع الشعار.
  - حقول واسعة بتباعد جيد، Labels فوق الحقول، وPlaceholder يشرح مثالاً.
  - رفع الشعار:
    - منطقة Drag & Drop مع أيقونة ملف، ونص يوضح الصيغ (AI، PDF، SVG) والحد الأقصى للحجم.
  - Checkbox للموافقة على شروط خدمة الطباعة إن وجدت.
  - زر "إرسال طلب عرض سعر" كبير وواضح، مع رسالة نجاح بعد الإرسال ورقم مرجعي للطلب.

---

### 6. صفحة السلة – `Cart`

- **التخطيط**:
  - في الديسكتوب: عمود لقائمة السلة، وعمود لملخص الطلب.
  - في الموبايل: قائمة السلة ثم الملخص، مع شريط سفلي ثابت لزر "متابعة للدفع".

- **بطاقة العنصر في السلة**:
  - صورة صغيرة للمنتج بحواف ناعمة.
  - اسم المنتج كرابط لصفحة التفاصيل.
  - سطر نصي صغير يظهر العرض المطبق (إن وجد).
  - مكوّن كمية (+ / -) بجانب السعر.
  - أيقونة إزالة بسيطة (سلة مهملات) في زاوية البطاقة.

- **ملخص السلة**:
  - بطاقة تحتوي على:
    - إجمالي المنتجات.
    - قيمة الخصومات.
    - تكلفة الشحن (مع شارة "مجاني" بلون أخضر عند تجاوز 299).
    - الإجمالي النهائي بخط أكبر وواضح.
  - زر "متابعة إلى الدفع" بارز بعرض البطاقة بالكامل.

- **سلة فارغة**:
  - أيقونة سلة فارغة + نص "سلتك فارغة".
  - زر "ابدأ التسوق الآن" يعيد المستخدم للرئيسية أو الكتالوج.

---

### 7. صفحة إتمام الطلب – `Checkout`

- **تجربة متعددة الخطوات**:
  - شريط أعلى يوضح الخطوات الأربع:
    - حساب.
    - عنوان.
    - شحن.
    - دفع.
  - تمييز الخطوة الحالية بلون واضح، والخطوات المكتملة بعلامة صح.

- **التصميم**:
  - كل خطوة داخل Card نظيفة، مع عنوان واضح ووصف مختصر.
  - ملخص الطلب دائماً ظاهر في يمين الصفحة أو أسفلها (حسب الجهاز).
  - أزرار "التالي" و"السابق" بتدرج بصري (التالي أوضح من السابق).

- **محتوى الخطوات**:
  - **الحساب**: خيارات لتسجيل الدخول، إنشاء حساب، وربما الاستمرار كضيف.
  - **العنوان**: بطاقات لعناوين العميل مع زر "إضافة عنوان جديد" يفتح نموذج بسيط.
  - **الشحن**: بطاقات لكل مزود شحن تعرض الاسم والمدة والسعر (مع إظهار "مجاني" عند تحقق الشرط).
  - **الدفع**:
    - بطاقات طرق الدفع مع شعارات رسمية (Mada، Visa، Apple Pay، Tamara).
    - عند اختيار Tamara، نص صغير يشرح "اشتر الآن وادفع لاحقاً".
    - زر "إكمال الدفع" يوجه إلى بوابة الدفع.

- **الشروط والأحكام**:
  - Checkbox للموافقة على الشروط وسياسة الخصوصية قبل الإكمال.
  - روابط للشروط وسياسة الخصوصية في نافذة جديدة أو Modal منسق.

---

### 8. صفحة حسابي – `My Account`

- **أقسام رئيسية**:
  - **ملفي الشخصي**:
    - عرض/تعديل الاسم، رقم الجوال، البريد الإلكتروني ضمن بطاقة أنيقة.
  - **عناويني**:
    - بطاقات للعناوين المسجلة مع أزرار "تعديل" و"حذف".
    - زر "إضافة عنوان" بارز.
  - **طلباتي**:
    - قائمة بالطلبات:
      - رقم الطلب، التاريخ، المبلغ، الحالة.
      - زر "عرض التفاصيل" يفتح صفحة/Modal بتفاصيل الطلب (منتجات، شحن، دفع، تتبع).
  - **طلبات الطباعة الخاصة** (للعملاء B2B):
    - قائمة بالطلبات B2B مع حالة كل طلب (تحت المراجعة، تم إرسال عرض سعر…).

---

### 9. صفحات المعلومات الثابتة – `Static Pages`

- **من نحن**:
  - قصة MJM، الرؤية، الرسالة، القيم مكتوبة بشكل تسويقي ومرتب.
  - صور مناسبة (المستودع، تغليف الطلبات، فريق العمل إن وجد).

- **سياسة الشحن والإرجاع**:
  - مناطق التغطية وأوقات التسليم.
  - تكاليف الشحن بالتفصيل مع إبراز الشحن المجاني فوق 299.
  - شروط الإرجاع والاستبدال (الفترة الزمنية، حالة المنتج…).

- **سياسة الخصوصية**:
  - طريقة جمع البيانات.
  - كيفية الاستخدام والحفظ.
  - حقوق المستخدم.

- **الشروط والأحكام**:
  - شروط استخدام الموقع.
  - المسؤوليات القانونية.

- **تواصل معنا**:
  - نموذج تواصل (اسم، إيميل، جوال، رسالة) بتصميم نظيف.
  - معلومات اتصال مباشرة (إيميل، رقم هاتف، واتساب، العنوان) في جانب/أسفل الصفحة.

---

## نموذج البيانات (Database Schema)

### جدول المنتجات `Products`

- `id` (PK).
- `name`.
- `description`.
- `category` (enum: `bags`, `water`, `carton`, `hospitality`, `bundle_component`).
- `price`.
- `discount_price` (اختياري).
- `weight` (اختياري).
- `gallon_capacity` (للمياه).
- `stock_quantity`.
- `sku`.
- `images` (جدول فرعي أو حقل JSON).
- `is_active` (boolean).
- `is_for_b2b_only` (boolean).

### جدول العروض `Offers`

- `id` (PK).
- `type` (enum: `B1G1`, `percentage`, `fixed_bundle_offer`, `second_item_for_1_sar`).
- `discount_value` (نسبة أو مبلغ).
- `start_date`.
- `end_date`.
- `is_active`.
- علاقة Many-to-Many مع `Products` عبر جدول وسيط `Offer_Products`:
  - `offer_id`, `product_id`.

### جدول الطلبات `Orders`

- `id` (PK).
- `customer_id` (FK → `Customers`).
- `status` (enum: `new`, `processing`, `packed`, `shipped`, `delivered`, `cancelled`).
- `total_amount`.
- `shipping_fee`.
- `discount_amount`.
- `payment_status` (enum: `pending`, `paid`, `failed`, `refunded`).
- `payment_method` (Mada, Apple Pay, Visa, Tamara, COD).
- `shipping_address_id` (FK → `Addresses`).
- `created_at`, `updated_at`.

### جدول عناصر الطلب `Order_Items`

- `id` (PK).
- `order_id` (FK).
- `product_id` (FK).
- `quantity`.
- `unit_price`.
- `final_price` (بعد الخصم/العرض).

### جدول طلبات الطباعة `Printing_Requests`

- `id` (PK).
- `customer_id` (FK).
- `business_type` (مطعم، كافيه، شركة…).
- `product_type` (نوع الكيس/المنتج).
- `logo_url`.
- `notes`.
- `status` (enum: `new`, `under_review`, `quoted`, `approved`, `rejected`, `completed`).
- `quoted_price`.
- `quote_currency` (SAR).
- `created_at`, `updated_at`.

### جدول اشتراكات / مبيعات المياه `Water_Subscription`

- `id` (PK).
- `customer_id` (FK).
- `type` (مرة واحدة / اشتراك شهري).
- `quantity_per_order`.
- `frequency` (أسبوعي / شهري).
- `next_delivery_date`.
- `status` (enum: `active`, `paused`, `cancelled`).

### جدول العملاء `Customers`

- `id` (PK).
- `name`.
- `email`.
- `phone`.
- `password_hash`.
- `is_business` (boolean).
- `business_name` (اختياري).
- `cr_number` (اختياري – السجل التجاري).
- `created_at`.

### جدول العناوين `Addresses`

- `id` (PK).
- `customer_id` (FK).
- `city`.
- `district`.
- `street`.
- `building_no`.
- `additional_info`.
- `is_default`.

### جدول مديري النظام `Admin_Users`

- `id` (PK).
- `name`.
- `email`.
- `password_hash`.
- `role` (enum: `super_admin`, `manager`, `staff`).
- `permissions` (JSON أو علاقة أدوار).

### جدول المحتوى `CMS_Content`

- `id`.
- `key` (مثل: `hero_banner`, `homepage_slider`, `maintenance_mode`, `seasonal_offer`).
- `value` (JSON أو HTML).
- `updated_by`.

### جداول الشحن (مقترحة)

- **`Shipping_Providers`**:
  - `id`, `name`, `type` (aramex, saee, box_company), `api_keys`.

- **`Shipments`**:
  - `id`.
  - `order_id` (FK).
  - `provider_id` (FK).
  - `tracking_number`.
  - `label_url`.
  - `status`.

---

## منطق الأعمال (Business Logic)

### عرض "القطعة الثانية بريال"

- **الحالة**: منتج عليه نوع عرض `second_item_for_1_sar`.
- **منطق الحساب**:
  - إذا `quantity >= 2`:
    - `pairs = floor(quantity / 2)`.
    - `remainder = quantity % 2`.
    - السعر:
      - `total = pairs * (price + 1) + remainder * price`.
- **التنفيذ في الباك إند**:
  - دالة تحسب إجمالي السلة:
    - تمر على كل `cart_item`.
    - إن كان مشمولاً بالعرض، تطبق المنطق أعلاه.
    - تعيد للفرونت تفاصيل:
      - السعر الأصلي.
      - الخصم الناتج عن العرض.
      - السعر النهائي.

### حساب الشحن

- **قاعدة عامة**:
  - إن كان `cart_total >= 299` → الشحن **مجاني**.
  - إن كان أقل:
    - تطبيق سعر ثابت (مثال 25 ريال) أو سياسة بحسب المدينة/الوزن (يتم الاتفاق عليها).

- **تكامل الشحن**:
  - عند تأكيد الطلب:
    - إرسال بيانات الشحنة للـ API:
      - الوزن التقريبي (حسب المنتجات).
      - أبعاد تقريبية إن لزم.
      - بيانات العنوان.
    - استقبال:
      - `tracking_number`.
      - `label_url`.
    - تخزين القيم في `Shipments`.

---

## لوحة تحكم الإدارة (MJM Control – Admin Dashboard)

### إدارة المحتوى (CMS)

- تحديث:
  - سلايدر الصفحة الرئيسية (صور + عناوين + أزرار).
  - أيقونات 3D للأقسام.
  - تشغيل/إيقاف:
    - وضع الصيانة.
    - العروض الموسمية.
- إدارة:
  - محتوى "من نحن"، "سياسة الشحن"، "سياسة الخصوصية"، "الشروط والأحكام".

### إدارة المنتجات والعروض

- **منتجات**:
  - إضافة / تعديل / حذف.
  - إدارة المخزون (الكمية المتاحة).
  - تفعيل/تعطيل منتج.

- **عروض**:
  - إنشاء عرض جديد:
    - نوع العرض، القيمة، المدة، المنتجات المشمولة.
  - تفعيل/إلغاء تفعيل.

### إدارة الطلبات

- شاشة طلبات مع:
  - فلترة حسب الحالة وطريقة الدفع ونوع العميل.
  - عرض معلومات الطلب كاملة.
  - تغيير حالة الطلب (جديد → قيد التغليف → مشحون → مكتمل).
  - إنشاء بوليصة شحن بضغطة واحدة (استدعاء API).
  - طباعة فاتورة PDF.
  - إرسال إشعار واتساب/إيميل من نفس الشاشة.

### إدارة طلبات الطباعة الخاصة

- عرض قائمة بجميع `Printing_Requests`.
- لكل طلب:
  - عرض بيانات العميل.
  - نوع النشاط، الكمية المتوقعة، نوع الكيس.
  - فتح/تحميل ملف الشعار.
  - إدخال سعر مقترح (`quoted_price`) وإرساله للعميل كفاتورة مخصصة.
  - تغيير الحالة بين (new، under_review، quoted، approved، completed، rejected).

### التحليلات (Analytics)

- لوحة تقارير تشمل:
  - **مبيعات يومية وشهرية** (رسم بياني).
  - **أكثر المنتجات طلباً** (أكياس النفايات غالباً في الصدارة).
  - **متوسط قيمة الطلب**.
  - **نسبة مبيعات البكجات** من إجمالي المبيعات.
  - **عدد طلبات الطباعة B2B** ومعدل تحويلها إلى صفقات مكتملة.

---

## الربط والتكامل (Integrations & APIs)

### بوابة الدفع (Moyasar / Tap + Tamara)

- **تدفق الدفع**:
  - من الباك إند:
    - إنشاء جلسة دفع (Payment Session) بالمبلغ الإجمالي.
  - من الفرونت إند:
    - إعادة توجيه العميل لصفحة الدفع الآمنة.
  - Webhooks:
    - استقبال إشعار تأكيد الدفع.
    - تحديث حالة الطلب إلى `paid`.

- **Tamara**:
  - خيار يظهر عند وصول السلة لمبلغ محدد (مثلاً 200 ريال).
  - تدفق خاص بها مع Webhooks لتأكيد الاستحقاق.

### خدمات الشحن (Aramex / ساعي / شركة الصندوق)

- **Aramex / ساعي**:
  - عنوان العميل + وزن الشحنة + مدينة الإرسال.
  - استلام:
    - رقم تتبع.
    - رابط بوليصة (PDF).
  - تخزين البيانات في `Shipments`.

- **شركة الصندوق للنقل الثقيل**:
  - إن توفر API:
    - نفس التدفق السابق مع وزن أكبر.
  - إن لم يتوفر:
    - إرسال طلب آلي (Email/WhatsApp) بتفاصيل الشحنة الكبيرة لمندوب الشركة.

### WhatsApp API

- رسائل آلية في الحالات التالية:
  - عند **تأكيد الطلب**.
  - عند **خروج الطلب للشحن**.
  - عند **وصول الطلب** (بناءً على Webhook من شركة الشحن أو تحديث يدوي من الأدمن).

- **أيقونة واتساب 3D عائمة**:
  - زر ثابت أسفل يمين الشاشة.
  - يفتح محادثة واتساب مع رقم الدعم.

---

## تحسين محركات البحث (SEO) والأداء (Performance)

### SEO

- **كلمات مفتاحية مستهدفة**:
  - أكياس نفايات جملة.
  - مفارش سفرة فاخرة.
  - طباعة أكياس مطاعم.
  - مياه MJM.

- **On-Page SEO**:
  - عناوين H1/H2/H3 منسقة لكل قسم.
  - Meta Title و Meta Description مخصصة لكل صفحة.
  - Slugs نظيفة وسهلة القراءة.

- **Schema Markup**:
  - `Product` لكل منتج:
    - `name`, `price`, `priceCurrency`, `image`, `availability`, `aggregateRating` إن توفر.
  - `Organization` لتعريف MJM في جوجل.

- **محتوى / مدونة (اختيارية)**:
  - مقالات قصيرة تسويقية وتعليمية مرتبطة بالكلمات المفتاحية.

### الأداء (Performance)

- صور **WebP** بأحجام مضغوطة ومدروسة.
- **Lazy Loading** للصور في الصفحة الطويلة.
- استخدام **CDN** لخدمة الصور والملفات الثابتة قرب المستخدمين في السعودية.
- تفعيل Caching للصفحات ذات الزيارات العالية (Next.js ISR أو مشابه).

---

## الهوية البصرية والعناصر الإبداعية

- **أيقونات 3D مخصصة**:
  - كيس نفايات Benefit (أسود/فضي لامع).
  - كرتون شحن.
  - زجاجة مياه MJM.
  - ملاعق وشوك ذهبية/فضية.

- **الهوية البصرية**:
  - شعار MJM بصيغة متجهة (SVG/AI).
  - لوحة ألوان أساسية (لون رئيسي + لون ثانوي + ألوان مساعدة).
  - اختيار خط عربي (مثلاً GE SS / Cairo / Tajawal) وخط لاتيني متناسق.

- **عناصر الثقة**:
  - شعار "معروف".
  - شعار "منصة الأعمال".
  - شعارات طرق الدفع (Mada، Visa، Apple Pay، Tamara).

---

## الأمان والامتثال (Security & Compliance)

- تفعيل **HTTPS** كاملاً.
- حفظ كلمات المرور باستخدام **bcrypt**.
- حماية لوحة التحكم:
  - تسجيل دخول آمن.
  - أدوار وصلاحيات دقيقة.
  - (اختياري) تفعيل 2FA للحسابات الحساسة.

- حماية الـ API:
  - استخدام **JWT** للجلسات.
  - Rate Limiting على مسارات حساسة (تسجيل دخول، إنشاء حساب).
  - التحقق من صحة البيانات (Validation) في كل Endpoint.

- الامتثال:
  - إعداد **سياسة خصوصية** متوافقة مع أنظمة المملكة.
  - توضيح **سياسة الإرجاع والاستبدال**.
  - توضيح **سياسة الشحن والتسليم**.

---

## خطة التنفيذ الزمنية (Roadmap)

### المرحلة 1: التحضير (1–2 أسابيع)

- حسم **الهوية البصرية** (شعار، ألوان، خطوط).
- كتابة المحتوى الأساسي:
  - نصوص الصفحة الرئيسية.
  - وصف مبدئي للمنتجات.
  - سياسات (شحن، إرجاع، خصوصية).
- تجهيز:
  - حسابات بوابات الدفع (Moyasar/Tap + Tamara).
  - حسابات شركات الشحن.
  - رقم واتساب رسمي للدعم.

### المرحلة 2: البنية الأساسية (3–4 أسابيع)

- إعداد بيئة التطوير (Frontend + Backend + DB).
- تنفيذ نموذج البيانات في PostgreSQL.
- بناء طبقة الـ API الأساسية:
  - المنتجات، العروض، السلة، الطلبات، العملاء.
- بناء الهيكل العام للواجهة مع Landing Page مبدئية.

### المرحلة 3: المنطق المتقدم والتكاملات (3–4 أسابيع)

- تطبيق منطق العروض (القطعة الثانية بريال، العروض الأخرى).
- ربط بوابة الدفع + Tamara.
- ربط على الأقل مزود شحن واحد كبداية (مثلاً أرامكس).
- بناء نسخة أولية من لوحة التحكم:
  - إدارة المنتجات.
  - إدارة الطلبات.
  - إدارة محتوى الصفحة الرئيسية.

### المرحلة 4: الاختبارات والتحسين (2–3 أسابيع)

- اختبار كامل لمسار:
  - تصفح → إضافة للسلة → تسجيل/دخول → إدخال عنوان → دفع → شحن.
- اختبار تجربة B2B للطباعة الخاصة.
- تحسينات أداء + SEO + تجربة المستخدم.
- إصلاح الأخطاء الظاهرة.

### المرحلة 5: الإطلاق والتشغيل

- إطلاق تجريبي (Soft Launch) لفئة محدودة من العملاء.
- جمع الملاحظات وتحسين سريع.
- إطلاق رسمي مع حملات تسويقية.

---

## ما المطلوب منك لتكتمل الخطة عملياً

- **الهوية البصرية**:
  - تأكيد إن كان لديك شعار جاهز وألوان محددة أم تحتاج اقتراحات.

- **سياسات المتجر**:
  - سياسة الشحن (مدن التغطية، مدة التوصيل، أسعار الشحن التفصيلية).
  - سياسة الإرجاع والاستبدال.

- **تفاصيل الشحن**:
  - هل الشحن سيكون بسعر ثابت لكل المملكة؟ أم يختلف حسب المدينة/الوزن؟
  - هل تريد **الدفع عند الاستلام (COD)** أم فقط دفع إلكتروني؟

- **المنتجات الأولية**:
  - قائمة مبدئية بـ 20–30 منتج:
    - اسم المنتج.
    - وصف مختصر.
    - سعر.
    - صورة (حتى لو مبدئية).

- **أولوية الإطلاق**:
  - أيهما أهم في البداية:
    - تجربة B2C كاملة؟
    - أم مسار B2B للطباعة الخاصة؟

